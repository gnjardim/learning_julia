# Use "Add Plots" in pkg> (press `]`) to install package. Press backspace to return to julia>
using Plots

# get help from function
?repeat
?replace
?plot

# plot sin
plot(sin, -2π, 2π, label="sin(x)")

# Check packages in current session "] st"